upgrade: true
dep: dev,prod
format: group
reject:
- '@types/react'
- '@types/react-dom'
- '@simplewebauthn/*' # TODO 升级 4 → 5
- 'codemirror' # TODO 升级 5 → 6
- 'react' # 先不升级 18
- 'react-dom' # 先不升级 18
- 'eslint' # 暂时停留在 8.22.0 23 在 webstorm 下报错 
- 'stylelint' # 14 不行
- 'stylelint-config-standard'
- 'stylelint-order'