{
  "npmClient": "yarn",
  "useWorkspaces": true,
  "packages": [
    "packages/*"
  ],
  "command": {
    "publish": {
      "npmClient": "npm",
      "ignoreChanges": ["stories/**", "tests/**", "*.md"],
      "allowBranch": [
        "lerna/pub",
        "lerna/pub-*"
      ]
    }
  },
  "version": "independent"
}
