export enum EAction {
  SET_DOM_UI,
  SET_DOM_TAB_LIST,
  SET_ACTIVE_TAB_KEY,
  SET_WIDTH,
  SET_NAV_OFFSET,
  SET_NAV_OFFSET_MAX
}

export enum ETabsVariant {
  PLAIN = 'plain',
  INVERSE = 'inverse'
}
