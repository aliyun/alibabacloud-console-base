export enum EMicroBrowserMode {
  FREE = 'free',
  PINNED = 'pinned',
  MINIMIZED = 'minimized'
}

export enum EAction {
  SET_MODE,
  REFRESH_WINDOW_SIZE,
  RND_RESIZE_START,
  RND_RESIZE,
  RND_DRAG_START,
  RND_DRAG_STOP,
  RESET
}
