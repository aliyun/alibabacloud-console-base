import {
  IModelState
} from '../types';

export const DEFAULT_CONTEXT_STATE: IModelState = {
  hovered: false,
  collapsed: false,
  filterValue: '',
  filterVisible: false,
  filterFocused: false
};
