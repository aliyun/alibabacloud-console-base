import {
  IModelState
} from '../types';

export const DEFAULT_CONTEXT_STATE: IModelState = {
  dockActive: false,
  dockActiveByHoverTimestamp: 0,
  dockHoverActiveTimer: null,
  dockLogoState: true
};
