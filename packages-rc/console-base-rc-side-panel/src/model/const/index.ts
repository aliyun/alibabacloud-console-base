import {
  IModelState
} from '../types';

export const BODY_CLASS_WITH_SIDE_PANEL = 'with-side-panel';

export const QUICK_TOP_VISIBLE_OFFSET = 200;

export const DEFAULT_CONTEXT_STATE: IModelState = {
  collapsed: false,
  quickTopVisible: false
};
