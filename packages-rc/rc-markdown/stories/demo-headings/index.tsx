import React from 'react';

import {
  HEADINGS
} from '../const';
import Demo from '../_demo';

export default function DemoHeadings(): JSX.Element {
  return <Demo {...{
    source: HEADINGS
  }} />;
}
