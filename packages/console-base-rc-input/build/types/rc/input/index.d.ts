import { IProps } from '../../types';
export default function Input({ block, round, borderless, disabled, innerLeft, innerRight, className, onFocus, onBlur, onChange, style, ...props }: IProps): JSX.Element;
