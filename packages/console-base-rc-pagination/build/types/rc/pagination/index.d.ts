import { IPropsPagination } from '../../types';
/**
 * 分页
 */
export default function Pagination({ page, pageSize, total, theme, hideWhenOne, onChange, ...props }: IPropsPagination): JSX.Element | null;
