/**
 * This util only available at prod runtime
 * 10-21
 * Although this method shares from widget to widget.
 * But it should stays with @alicloud/widget-utils-config
 * Consider with the upgrade thing, this method stays
 * in @alicloud/widget-utils-console for now.
 * 
 * It will be reorganized to @alicloud/widget-utils-config
 * when the time is fit.
 */

function getWidgetInfo() {
  const info = {}
  return info
}

export default getWidgetInfo
