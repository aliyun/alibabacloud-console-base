import {
  css
} from 'styled-components';

import {
  SHADOW
} from '../theme-default';

export const mixinShadowS = css`
  box-shadow: ${SHADOW.S};
  box-shadow: var(--cb-shadow-s, ${SHADOW.S});
`;
export const mixinShadowSUp = css`
  box-shadow: ${SHADOW.S_UP};
  box-shadow: var(--cb-shadow-s-up, ${SHADOW.S_UP});
`;
export const mixinShadowSRight = css`
  box-shadow: ${SHADOW.S_RIGHT};
  box-shadow: var(--cb-shadow-s-right, ${SHADOW.S_RIGHT});
`;
export const mixinShadowSDown = css`
  box-shadow: ${SHADOW.S_DOWN};
  box-shadow: var(--cb-shadow-s-down, ${SHADOW.S_DOWN});
`;
export const mixinShadowSLeft = css`
  box-shadow: ${SHADOW.S_LEFT};
  box-shadow: var(--cb-shadow-s-left, ${SHADOW.S_LEFT});
`;
export const mixinShadowM = css`
  box-shadow: ${SHADOW.M};
  box-shadow: var(--cb-shadow-m, ${SHADOW.M});
`;
export const mixinShadowMUp = css`
  box-shadow: ${SHADOW.M_UP};
  box-shadow: var(--cb-shadow-m-up, ${SHADOW.M_UP});
`;
export const mixinShadowMRight = css`
  box-shadow: ${SHADOW.M_RIGHT};
  box-shadow: var(--cb-shadow-m-right, ${SHADOW.M_RIGHT});
`;
export const mixinShadowMDown = css`
  box-shadow: ${SHADOW.M_DOWN};
  box-shadow: var(--cb-shadow-m-down, ${SHADOW.M_DOWN});
`;
export const mixinShadowMLeft = css`
  box-shadow: ${SHADOW.M_LEFT};
  box-shadow: var(--cb-shadow-m-left, ${SHADOW.M_LEFT});
`;
export const mixinShadowL = css`
  box-shadow: ${SHADOW.L};
  box-shadow: var(--cb-shadow-l, ${SHADOW.L});
`;
export const mixinShadowLUp = css`
  box-shadow: ${SHADOW.L_UP};
  box-shadow: var(--cb-shadow-l-up, ${SHADOW.L_UP});
`;
export const mixinShadowLRight = css`
  box-shadow: ${SHADOW.L_RIGHT};
  box-shadow: var(--cb-shadow-l-right, ${SHADOW.L_RIGHT});
`;
export const mixinShadowLDown = css`
  box-shadow: ${SHADOW.L_DOWN};
  box-shadow: var(--cb-shadow-l-down, ${SHADOW.L_DOWN});
`;
export const mixinShadowLLeft = css`
  box-shadow: ${SHADOW.L_LEFT};
  box-shadow: var(--cb-shadow-l-left, ${SHADOW.L_LEFT});
`;
export const mixinShadowXl = css`
  box-shadow: ${SHADOW.XL};
  box-shadow: var(--cb-shadow-xl, ${SHADOW.XL});
`;
export const mixinShadowXlUp = css`
  box-shadow: ${SHADOW.XL_UP};
  box-shadow: var(--cb-shadow-xl-up, ${SHADOW.XL_UP});
`;
export const mixinShadowXlRight = css`
  box-shadow: ${SHADOW.XL_RIGHT};
  box-shadow: var(--cb-shadow-xl-right, ${SHADOW.XL_RIGHT});
`;
export const mixinShadowXlDown = css`
  box-shadow: ${SHADOW.XL_DOWN};
  box-shadow: var(--cb-shadow-xl-down, ${SHADOW.XL_DOWN});
`;
export const mixinShadowXlLeft = css`
  box-shadow: ${SHADOW.XL_LEFT};
  box-shadow: var(--cb-shadow-xl-left, ${SHADOW.XL_LEFT});
`;
