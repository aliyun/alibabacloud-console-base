import { IDialogPropsMutable } from '../../types';
import { IContextState } from '../types';
/**
 * 有限更新 props
 */
export default function reduceUpdateProps(state: IContextState, payload: IDialogPropsMutable): IContextState;
